name: wapnet
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.8
  - pip
  - pip:
      - torch==2.3.0
      - torchvision
      - lightning
      - numpy
      - opencv-python
      - pillow
      - scikit-image
      - scipy
      - tqdm
      - einops
      - matplotlib
      - PyYAML
      - thop
      - ptflops
      - pyiqa
